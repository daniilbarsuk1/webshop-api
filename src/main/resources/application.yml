spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/webshop
    username: postgres
    password: 111111
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update